// app/components/weather/layout.tsx

// Layout for the weather component route
export default function WeatherLayout({ children }: { children: React.ReactNode }) {
    return <div>{children}</div>; // Simple wrapper around the weather component
  }
  